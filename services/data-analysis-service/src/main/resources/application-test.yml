# 测试环境配置
spring:
  datasource:
    url: jdbc:mysql://test-mysql:3306/live_analysis_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=UTC
    username: ${DB_USER:root}
    password: ${DB_PASS:root}
  
  redis:
    host: test-redis
    port: 6379
    database: 3

# 日志级别
logging:
  level:
    root: INFO
    com.liveroom.analysis: INFO
