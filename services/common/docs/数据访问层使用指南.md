# 📚 Common 模块数据访问层完整指南

## 🎯 概述

从本版本开始，**所有微服务模块必须通过 Common 模块的数据访问层来读写数据库**。这保证了：

✅ **数据一致性** - 统一的数据访问接口
✅ **缓存管理** - 自动的 Redis 缓存策略  
✅ **批量操作** - 高效的批量读写优化
✅ **事务管理** - 自动的事务控制和回滚
✅ **数据格式** - 统一的数据格式和转换

---

## 📁 模块结构

### Repository 层（数据访问接口）

```
common/repository/
├── BaseRepository.java           # 基础Repository接口
├── UserRepository.java           # 用户Repository
├── AnchorRepository.java         # 主播Repository
├── AudienceRepository.java       # 观众Repository
├── LiveRoomRepository.java       # 直播间Repository
├── RechargeRepository.java       # 打赏Repository（核心业务）
├── SettlementRepository.java     # 结算Repository
├── WithdrawalRepository.java     # 提现Repository
└── CommissionRateRepository.java # 分成比例Repository
```

### Service 层（业务逻辑和缓存）

```
common/service/
├── BaseService.java              # 基础Service类（含缓存和批量操作）
├── UserService.java              # 用户Service
├── AnchorService.java            # 主播Service
├── AudienceService.java          # 观众Service
├── LiveRoomService.java          # 直播间Service
├── RechargeService.java          # 打赏Service（含幂等性控制）
├── SettlementService.java        # 结算Service
├── WithdrawalService.java        # 提现Service（含幂等性控制）
├── CommissionRateService.java    # 分成比例Service
└── DataAccessFacade.java         # 数据访问门面（统一入口）⭐
```

---

## 🚀 快速开始

### 步骤 1：注入 DataAccessFacade

```java
import common.service.DataAccessFacade;
import org.springframework.beans.factory.annotation.Autowired;

@RestController
@RequestMapping("/api/user")
public class UserController {
    
    @Autowired
    private DataAccessFacade dataAccessFacade;
}
```

### 步骤 2：通过门面访问数据

```java
// 用户操作
public void handleUserOperation() {
    // 单条查询
    User user = dataAccessFacade.user()
        .findById(userId)
        .orElse(null);
    
    // 批量查询
    List<User> users = dataAccessFacade.user()
        .findAll();
    
    // 创建用户
    User newUser = dataAccessFacade.user()
        .createUser(user);
    
    // 批量创建
    List<User> savedUsers = dataAccessFacade.user()
        .batchSaveUsers(userList);
}
```

### 步骤 3：访问其他实体

```java
// 主播操作
Anchor anchor = dataAccessFacade.anchor()
    .findByUserId(userId)
    .orElse(null);

// 打赏操作
Recharge recharge = dataAccessFacade.recharge()
    .createRecharge(rechargeRecord);

// 结算操作
Settlement settlement = dataAccessFacade.settlement()
    .findByAnchorId(anchorId)
    .orElse(null);

// 提现操作
Withdrawal withdrawal = dataAccessFacade.withdrawal()
    .createWithdrawal(withdrawalRecord);
```

---

## 📖 核心功能详解

### 1. 基础 CRUD 操作

所有 Service 都继承 `BaseService`，提供通用的增删改查操作：

```java
// 查询单个
Optional<T> findById(ID id);

// 查询所有
List<T> findAll();

// 分页查询
Page<T> findPage(int pageNum, int pageSize);

// 保存
T save(T entity);

// 更新
T update(T entity);

// 删除
void deleteById(ID id);
```

### 2. 批量操作

**高效的批量读写操作：**

```java
// 批量查询（带缓存）
List<User> users = dataAccessFacade.user()
    .findBatch(List.of(1L, 2L, 3L));

// 批量保存（自动拆分为 500 条一批）
List<User> saved = dataAccessFacade.user()
    .saveBatch(userList);

// 批量更新
List<Anchor> updated = dataAccessFacade.anchor()
    .updateBatch(anchorList);

// 批量删除
dataAccessFacade.user()
    .deleteBatch(List.of(1L, 2L, 3L));
```

### 3. Redis 缓存策略

所有 Service 都自动使用 Redis 缓存：

```java
// 第一次查询从数据库获取，并缓存
User user = dataAccessFacade.user()
    .findById(1L)
    .orElse(null);  // 从 DB，缓存到 Redis

// 第二次查询直接从缓存获取（毫秒级）
User user2 = dataAccessFacade.user()
    .findById(1L)
    .orElse(null);  // 从 Redis

// 更新时自动清除相关缓存
dataAccessFacade.user()
    .updateUser(user);  // 自动清除缓存
```

**缓存键规则：**
- 用户: `cache:user::` + `userId`
- 主播: `cache:anchor::` + `anchorId` 
- 观众: `cache:audience::` + `audienceId`
- 打赏: `cache:recharge::` + `rechargeId`
- 结算: `cache:settlement::` + `settlementId`

### 4. 幂等性控制（防重复）

打赏和提现操作支持幂等性控制：

```java
// 通过 traceId 防止重复提交
public void processRecharge(Recharge recharge) {
    // 第一次提交
    Recharge result1 = dataAccessFacade.recharge()
        .createRecharge(recharge);  // 创建成功
    
    // 第二次重复提交（相同 traceId）
    Recharge result2 = dataAccessFacade.recharge()
        .createRecharge(recharge);  // 返回已存在的记录，不重复创建
}

// 检查是否存在
boolean exists = dataAccessFacade.recharge()
    .existsByTraceId(traceId);
```

### 5. 事务管理

```java
// 批量操作自动在事务内执行
dataAccessFacade.beginBatchOperation(facade -> {
    // 创建用户
    User user = facade.user().createUser(userData);
    
    // 创建观众
    Audience audience = facade.audience().createAudience(audienceData);
    
    // 创建打赏
    Recharge recharge = facade.recharge().createRecharge(rechargeData);
    
    // 如果任何操作失败，整个事务回滚
});
```

---

## 🎓 常见场景

### 场景 1: 用户注册流程

```java
public BaseResponse<UserDTO> registerUser(UserRegisterRequest request) {
    DataAccessFacade data = dataAccessFacade;
    
    // 1. 检查用户是否存在
    if (data.user().checkUsernameExists(request.getUsername())) {
        throw new BusinessException("用户名已存在");
    }
    
    // 2. 创建用户（在事务内）
    data.beginBatchOperation(facade -> {
        // 创建基础用户
        User user = new User();
        user.setUsername(request.getUsername());
        user.setEmail(request.getEmail());
        User savedUser = facade.user().createUser(user);
        
        // 创建观众信息
        Audience audience = new Audience();
        audience.setUserId(savedUser.getUserId());
        facade.audience().createAudience(audience);
    });
    
    return ResponseUtil.success("注册成功");
}
```

### 场景 2: 打赏处理（含结算）

```java
public BaseResponse<Void> processRecharge(RechargeDTO dto) {
    // 防重复提交
    if (dataAccessFacade.recharge().existsByTraceId(dto.getTraceId())) {
        return ResponseUtil.success("打赏已处理");
    }
    
    dataAccessFacade.beginBatchOperation(facade -> {
        // 1. 创建打赏记录
        Recharge recharge = new Recharge();
        recharge.setTraceId(dto.getTraceId());
        recharge.setAnchorId(dto.getAnchorId());
        recharge.setAudienceId(dto.getAudienceId());
        recharge.setRechargeAmount(dto.getAmount());
        facade.recharge().createRecharge(recharge);
        
        // 2. 更新观众消费数据
        facade.audience().incrementRecharge(
            dto.getAudienceId(), 
            dto.getAmount(), 
            1L
        );
        
        // 3. 更新主播收益
        facade.anchor().incrementEarnings(
            dto.getAnchorId(), 
            dto.getAmount()
        );
        
        // 4. 更新直播间数据
        facade.liveRoom().incrementRevenueAndRecharge(
            dto.getLiveRoomId(), 
            dto.getAmount()
        );
    });
    
    return ResponseUtil.success("打赏处理成功");
}
```

### 场景 3: 批量结算

```java
public void settlementJob() {
    // 1. 查询待结算的主播
    List<Settlement> pendingList = dataAccessFacade.settlement()
        .findPendingSettlements(LocalDateTime.now());
    
    if (pendingList.isEmpty()) {
        return;
    }
    
    List<Long> anchorIds = pendingList.stream()
        .map(Settlement::getAnchorId)
        .collect(Collectors.toList());
    
    // 2. 批量查询打赏数据
    List<Recharge> recharges = dataAccessFacade.recharge()
        .findUnsettledRechargeBatch(anchorIds);
    
    // 3. 分组计算
    Map<Long, BigDecimal> settlementMap = recharges.stream()
        .collect(Collectors.groupingBy(
            Recharge::getAnchorId,
            Collectors.reducing(
                BigDecimal.ZERO,
                Recharge::getRechargeAmount,
                BigDecimal::add
            )
        ));
    
    // 4. 批量更新结算信息（在事务内）
    dataAccessFacade.beginBatchOperation(facade -> {
        settlementMap.forEach((anchorId, amount) -> {
            facade.settlement().incrementAvailableAmount(anchorId, amount);
        });
    });
}
```

### 场景 4: 查询用户排行榜

```java
public List<AnchorDTO> getTopAnchors(int limit) {
    // 直接查询排行榜（带缓存）
    List<Anchor> anchors = dataAccessFacade.anchor()
        .findTopAnchorsByEarnings(limit);
    
    return BeanUtil.convertList(anchors, AnchorDTO.class);
}
```

---

## 🔒 安全性和约束

### 1. 不允许直接使用 Repository

❌ **禁止：**
```java
@Autowired
private UserRepository userRepository;  // 不要这样做！

userRepository.findById(id);  // 绕过了 Service 层的验证和缓存
```

✅ **推荐：**
```java
@Autowired
private DataAccessFacade dataAccessFacade;

dataAccessFacade.user().findById(id);  // 使用门面
```

### 2. 数据格式统一

所有返回给前端的数据必须通过 DTO 转换：

```java
// ✅ 正确
User user = dataAccessFacade.user().findById(id).orElse(null);
UserDTO dto = BeanUtil.convert(user, UserDTO.class);
return ResponseUtil.success(dto);

// ❌ 错误
User user = dataAccessFacade.user().findById(id).orElse(null);
return ResponseUtil.success(user);  // 直接返回 Entity
```

### 3. 批量操作限制

- 单次批量操作建议不超过 1000 条
- 自动拆分为 500 条一批处理
- 如超过 1000 条，建议分多个批次

```java
// ✅ 推荐
List<User> users = new ArrayList<>();
for (int i = 0; i < 50000; i += 1000) {
    List<User> batch = users.subList(i, Math.min(i + 1000, users.size()));
    dataAccessFacade.user().saveBatch(batch);
}

// ❌ 不推荐
dataAccessFacade.user().saveBatch(50000条数据);  // 容易 OOM
```

---

## 📊 性能优化建议

### 1. 利用缓存

```java
// 多次访问同一数据时，自动使用缓存
User user = dataAccessFacade.user().findById(id);  // 查询 DB
User user2 = dataAccessFacade.user().findById(id);  // 查询缓存（快速）
```

### 2. 使用分页

```java
// 避免一次性查询大量数据
Page<User> page = dataAccessFacade.user()
    .findPage(1, 20);
```

### 3. 批量操作

```java
// 用批量操作替代循环单条操作
// ❌ 低效（1000 次 DB 操作 + 1000 次缓存操作）
for (User u : users) {
    dataAccessFacade.user().save(u);
}

// ✅ 高效（1 次 DB 操作 + 1 次缓存操作）
dataAccessFacade.user().saveBatch(users);
```

---

## 🐛 故障排查

### 问题 1：数据不一致

**症状：** 修改数据后，查询还是旧数据

**原因：** 未及时清除缓存

**解决：**
```java
// Service 会自动清除缓存，确保是最新操作
dataAccessFacade.user().updateUser(user);  // 自动清除缓存

// 如需手动清除
dataAccessFacade.clearAllCache();
```

### 问题 2：重复提交

**症状：** 多次提交打赏，重复入账

**原因：** 没有使用幂等性控制

**解决：**
```java
// 使用 traceId 进行幂等性控制
Recharge recharge = new Recharge();
recharge.setTraceId(generateTraceId());  // 必须生成唯一 ID
dataAccessFacade.recharge().createRecharge(recharge);
```

### 问题 3：事务回滚

**症状：** 部分操作成功，部分失败

**原因：** 没有使用事务包装

**解决：**
```java
// 使用事务包装，确保原子性
dataAccessFacade.beginBatchOperation(facade -> {
    // 所有操作要么全部成功，要么全部失败
    facade.user().createUser(user);
    facade.audience().createAudience(audience);
});
```

---

## 📝 总结

| 操作 | 示例 | 特点 |
|-----|------|------|
| 单条查询 | `findById(id)` | 带缓存，快速 |
| 批量查询 | `findBatch(ids)` | 高效，自动缓存 |
| 分页查询 | `findPage(pageNum, pageSize)` | 适合大数据集 |
| 创建 | `save(entity)` | 自动清除缓存 |
| 批量创建 | `saveBatch(list)` | 高效，自动拆分 |
| 更新 | `update(entity)` | 自动清除缓存 |
| 删除 | `deleteById(id)` | 自动清除缓存 |
| 幂等性 | `existsByTraceId(traceId)` | 防重复，仅打赏/提现 |
| 事务批量 | `beginBatchOperation()` | 原子性，全部或全部不 |

---

## ✅ 检查清单

在集成 Common 模块的数据访问层前，请确保：

- [ ] 移除所有直接使用 Repository 的代码
- [ ] 统一使用 DataAccessFacade
- [ ] 所有返回前端的数据都使用 DTO 转换
- [ ] 打赏/提现操作使用 traceId 进行幂等性控制
- [ ] 批量操作不超过 1000 条
- [ ] 关键业务操作使用事务包装
- [ ] 定期检查缓存命中率
- [ ] 监控 Redis 内存使用情况

---

## 📞 技术支持

如有问题，请联系：
- **架构师**：提交 Issue 或 Pull Request
- **文档**：查看 common/docs/ 目录
- **示例**：查看各微服务的集成代码

---

最后更新：2026年1月6日  
版本：1.0.0
