# 直播系统 API 接口总汇

## 项目概述

这是一个基于微服务架构的直播平台系统，包含7个核心微服务模块，提供完整的直播、打赏、财务、分析等功能。

---

## 服务架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                       Client (前端)                          │
├─────────────────────────────────────────────────────────────┤
│                      Nginx (网关层)                          │
├──────────────┬──────────────┬──────────────┬────────────────┤
│ Anchor       │ Audience     │ Finance      │ Data-Analysis  │
│ Service      │ Service      │ Service      │ Service        │
│ (8081)       │ (8082)       │ (8083)       │ (8084)         │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ Redis        │ DB-Service   │ Mock-Service │                │
│ Service      │ (8086)       │ (8087)       │                │
│ (8085)       │              │              │                │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

---

## 服务清单

| 序号 | 服务名称 | 端口 | 基础路径 | 功能描述 |
|------|---------|------|---------|---------|
| 1 | anchor-service | 8081 | /anchor/api/v1 | 主播管理、直播间管理 |
| 2 | audience-service | 8082 | /audience/api/v1 | 观众管理、打赏管理 |
| 3 | finance-service | 8083 | /finance/api/v1 | 财务管理、结算统计 |
| 4 | data-analysis-service | 8084 | /analysis/api/v1 | 数据分析、排行榜 |
| 5 | redis-service | 8085 | /redis/api/v1 | 缓存、分布式锁 |
| 6 | db-service | 8086 | /api/database | 数据库初始化管理 |
| 7 | mock-service | 8087 | /api/v1/mock | 测试数据生成 |

---

## API 文档导航

### 1. 主播服务 (Anchor Service)
📄 详细文档: [1-anchor-service-API.md](./1-anchor-service-API.md)

**主要功能**:
- ✅ 主播账户管理（创建、查询、更新）
- ✅ 直播间生命周期管理（开启、结束、更新）
- ✅ 打赏记录查询
- ✅ 提现申请管理
- ✅ 实时数据处理（观众进出、弹幕、打赏）
- ✅ 分成比例查询

**核心端点**:
```
POST   /api/v1/anchors                          - 创建主播
GET    /api/v1/anchors/{anchorId}               - 查询主播信息
PUT    /api/v1/anchors/{anchorId}               - 更新主播信息
GET    /api/v1/live-rooms/{liveRoomId}          - 查询直播间
POST   /api/v1/live-rooms/{liveRoomId}/start    - 开启直播
POST   /api/v1/live-rooms/{liveRoomId}/end      - 结束直播
```

---

### 2. 观众服务 (Audience Service)
📄 详细文档: [2-audience-service-API.md](./2-audience-service-API.md)

**主要功能**:
- ✅ 观众账户管理（注册、游客模式、查询、禁用）
- ✅ 打赏记录管理（创建、查询、同步）
- ✅ 消费统计
- ✅ 与财务服务的数据同步
- ✅ 幂等性检查（防重复打赏）

**核心端点**:
```
POST   /api/v1/audiences                        - 创建观众
POST   /api/v1/audiences/guest                  - 创建游客
GET    /api/v1/audiences/{audienceId}           - 查询观众信息
POST   /api/v1/recharge                         - 创建打赏
GET    /api/v1/recharge/anchor/{anchorId}       - 查询主播打赏列表
```

---

### 3. 财务服务 (Finance Service)
📄 详细文档: [3-finance-service-API.md](./3-finance-service-API.md)

**主要功能**:
- ✅ 提现申请审核（批准、拒绝）
- ✅ 结算管理（查询、手动触发）
- ✅ 收入统计分析
- ✅ 分成比例管理
- ✅ 数据同步接收

**核心端点**:
```
POST   /api/v1/withdrawal                       - 申请提现
GET    /api/v1/withdrawal/{anchorId}            - 查询提现记录
PUT    /api/v1/withdrawal/{withdrawalId}/approve - 批准提现
GET    /api/v1/statistics/anchor/revenue/{anchorId} - 查询收入统计
```

---

### 4. 数据分析服务 (Data Analysis Service)
📄 详细文档: [4-data-analysis-service-API.md](./4-data-analysis-service-API.md)

**主要功能**:
- ✅ 聚合统计分析
- ✅ 主播收入分析与排行榜
- ✅ 时间序列数据
- ✅ 用户画像与行为分析
- ✅ 保留率分析
- ✅ 热力图分析

**核心端点**:
```
GET    /api/analysis/aggregation/metrics       - 获取关键指标
GET    /api/v1/analysis/anchor/income/{anchorId} - 主播收入分析
GET    /api/analysis/timeseries/daily          - 每日时间序列
GET    /api/v1/analysis/user/{userId}          - 用户画像
```

---

### 5. Redis服务 (Redis Service)
📄 详细文档: [5-redis-service-API.md](./5-redis-service-API.md)

**主要功能**:
- ✅ 缓存管理（设置、获取、删除、过期）
- ✅ 分布式锁（获取、释放、强制释放）
- ✅ 幂等性检查
- ✅ 计数器操作（自增、自减）

**核心端点**:
```
POST   /api/v1/cache/set                       - 设置缓存
GET    /api/v1/cache/get                       - 获取缓存
POST   /api/v1/lock/try-lock                   - 获取锁
POST   /api/v1/lock/release-lock               - 释放锁
```

---

### 6. 数据库服务 (Database Service)
📄 详细文档: [6-db-service-API.md](./6-db-service-API.md)

**主要功能**:
- ✅ 数据库健康检查
- ✅ 数据库初始化
- ✅ 表完整性检查
- ✅ 服务信息查询

**核心端点**:
```
GET    /api/database/health                    - 健康检查
POST   /api/database/initialize                - 初始化数据库
GET    /api/database/tables                    - 检查表
GET    /api/database/info                      - 获取服务信息
```

---

### 7. 模拟数据服务 (Mock Service)
📄 详细文档: [7-mock-service-API.md](./7-mock-service-API.md)

**主要功能**:
- ✅ 模拟主播、观众、打赏数据生成
- ✅ 批量生成测试数据
- ✅ 直播模拟
- ✅ 数据清理与重置

**核心端点**:
```
GET    /api/v1/mock/data/statistics            - 查询统计
POST   /api/v1/mock/anchor/create              - 创建主播
POST   /api/v1/mock/anchor/batch-create        - 批量创建主播
POST   /api/v1/mock/recharge/batch-create      - 批量创建打赏
```

---

## 通用API规范

### 响应格式

所有服务都使用统一的JSON响应格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 状态码说明

| 代码 | 含义 | 说明 |
|------|------|------|
| 200 | OK | 请求成功 |
| 400 | Bad Request | 参数错误 |
| 401 | Unauthorized | 未授权 |
| 403 | Forbidden | 禁止访问 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 冲突（如重复操作） |
| 500 | Server Error | 服务器错误 |
| 503 | Service Unavailable | 服务不可用 |

### 时间格式

所有时间参数使用 **ISO 8601** 格式：
```
2024-01-07T10:30:00
2024-01-07T10:30:00Z
```

### 金额格式

所有金额参数使用 **BigDecimal**，精确到两位小数：
```
100.00
50.50
0.01
```

### 分页参数

标准分页参数：
- `page`: Integer (默认1，最小1)
- `size`: Integer (默认20，范围1-100)
- `sortBy`: String (可选，排序字段)
- `sortOrder`: String (可选，asc/desc，默认desc)

### 幂等性

以下操作支持幂等性，通过 `traceId` 实现：
- 创建打赏 (`POST /api/v1/recharge`)
- 申请提现 (`POST /api/v1/withdrawal`)
- 接收批量数据 (`POST /internal/sync/recharges`)

幂等性超时时间：
- 打赏：60秒
- 提现：60秒
- 数据同步：需要通过batchId

---

## 核心业务流程

### 1. 直播流程

```
1. 主播创建账户 (POST /anchor/api/v1/anchors)
2. 自动创建直播间
3. 主播开启直播 (POST /anchor/api/v1/live-rooms/{id}/start)
4. 观众进入直播间 (POST /anchor/api/v1/live-rooms/realtime/viewer-enter)
5. 观众打赏 (POST /audience/api/v1/recharge)
6. 财务服务同步数据 (POST /finance/internal/sync/recharges)
7. 主播结束直播 (POST /anchor/api/v1/live-rooms/{id}/end)
8. 结算数据 (自动或手动触发)
```

### 2. 提现流程

```
1. 主播查询可提取金额 (GET /finance/api/v1/settlement/{id}/balance)
2. 主播申请提现 (POST /finance/api/v1/withdrawal)
3. 财务审核批准 (PUT /finance/api/v1/withdrawal/{id}/approve)
4. 后台异步处理打款
5. 主播查询提现记录 (GET /finance/api/v1/withdrawal/{id})
```

### 3. 数据同步流程

```
观众服务产生打赏数据
    ↓
发送到消息队列
    ↓
定时任务触发同步
    ↓
调用财务服务 /internal/sync/recharges
    ↓
财务服务处理并存储
    ↓
观众服务标记为已同步
```

---

## 缓存策略

### 热数据缓存 (使用Redis)

| 数据 | TTL | 更新频率 |
|------|-----|---------|
| 用户信息 | 1小时 | 用户修改时刷新 |
| 主播信息 | 30分钟 | 每30分钟刷新 |
| 直播间信息 | 5分钟 | 实时更新 |
| 排行榜数据 | 5分钟 | 每5分钟更新 |
| 在线人数 | 实时 | 每秒更新 |

### 分布式锁使用场景

| 场景 | 锁键 | 超时时间 |
|------|------|---------|
| 提现处理 | finance:lock:withdrawal | 30秒 |
| 结算计算 | finance:lock:settlement | 60秒 |
| 数据同步 | sync:lock:recharge | 30秒 |

---

## Nginx 配置

详见: [NGINX接口汇总.md](../nginx/NGINX接口汇总.md)

Nginx作为反向代理网关，统一管理所有微服务的路由。

---

## Docker 部署

### 快速启动所有服务

```bash
# 进入部署目录
cd deployment/docker

# 启动所有服务
./manage.sh start

# 检查服务状态
./manage.sh status

# 停止所有服务
./manage.sh stop
```

### 逐个服务启动

```bash
# 启动特定服务
docker-compose up -d anchor-service
docker-compose up -d audience-service
docker-compose up -d finance-service
docker-compose up -d data-analysis-service
docker-compose up -d redis-service
docker-compose up -d db-service
docker-compose up -d mock-service
docker-compose up -d nginx
```

---

## 监控与健康检查

### 健康检查端点

```bash
# Anchor Service
curl http://localhost:8081/anchor/api/v1/health

# Audience Service
curl http://localhost:8082/audience/api/v1/health

# Finance Service
curl http://localhost:8083/finance/api/v1/health

# Data Analysis Service
curl http://localhost:8084/analysis/health

# Redis Service
curl http://localhost:8085/redis/api/v1/cache/health

# DB Service
curl http://localhost:8086/api/database/health

# Mock Service
curl http://localhost:8087/api/v1/health
```

### Kubernetes 健康检查配置

```yaml
livenessProbe:
  httpGet:
    path: /{service}/health
    port: {port}
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /{service}/health
    port: {port}
  initialDelaySeconds: 10
  periodSeconds: 5
```

---

## 数据库表清单

### 核心业务表
- **anchor**: 主播信息
- **audience**: 观众信息
- **live_room**: 直播间信息
- **recharge**: 打赏记录
- **withdrawal**: 提现申请
- **settlement**: 结算记录
- **commission_rate**: 分成比例

### 系统表
- **audit_log**: 审计日志
- **system_config**: 系统配置
- **data_dict**: 数据字典

---

## 常见问题解答

### Q: 如何调试服务间通信？
A: 使用Redis服务的分布式锁和缓存功能，通过日志追踪traceId。

### Q: 如何确保数据一致性？
A: 使用幂等性检查、分布式锁、事务机制。详见各服务文档。

### Q: 如何处理高并发？
A: 
1. 使用Redis缓存减少数据库访问
2. 使用消息队列异步处理
3. 使用分布式锁保证数据一致性

### Q: 如何监控系统性能？
A: 定期查看数据分析服务的统计数据，监控各服务的健康状态。

---

## 技术栈

- **框架**: Spring Boot 2.x + Spring Cloud
- **数据库**: MySQL 8.0+
- **缓存**: Redis 6.0+
- **消息队列**: RabbitMQ/Kafka
- **API网关**: Nginx
- **容器**: Docker + Docker Compose
- **编程语言**: Java 11+

---

## 文档维护

本API文档定期更新，最后更新时间：**2024年1月7日**

所有API接口都遵循RESTful规范，使用标准的HTTP方法和状态码。

---

## 相关文档

- 📘 [POM 详细解释](../POM_详细解释.md)
- 📙 [Redis 配置指南](../REDIS_CONFIGURATION_GUIDE.md)
- 📕 [系统集成与打赏流程优化](../系统集成与打赏流程优化总结.md)
- 📗 [Nginx 接口汇总](../nginx/NGINX接口汇总.md)
- 📔 [Docker 快速启动](../deployment/docker/QUICKSTART.md)

---

## 支持与反馈

如有任何问题或建议，请联系开发团队。

